{
  "name": "snapsave-pro-universal",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "node scripts/setup.js",
    "preinstall": "mkdir -p .local_bin && (ln -sf $(which python3 || which python) .local_bin/python || true)",
    "postinstall": "cd frontend && npm install",
    "dev": "npm run setup && concurrently \"nodemon backend/server.js\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm install && npm run build",
    "start": "export PATH=\"$(pwd)/.local_bin:$PATH\" && npm run setup && node backend/server.js"
  },
  "dependencies": {
    "axios": "^1.13.4",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "morgan": "^1.10.1",
    "yt-dlp-exec": "^1.0.2"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.11"
  }
}