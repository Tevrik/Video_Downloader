{
  "name": "snapsave-pro-universal",
  "private": true,
  "type": "module",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "setup": "node scripts/setup.js",
    "install:all": "npm install",
    "dev": "npm run setup && concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w frontend",
    "start": "export PATH=\"$(pwd)/.local_bin:$PATH\" && npm run setup && npm run start -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}